// Logical operators
// (|| - OR), (&& - AND), !==> NOT

const name = "Bob";
const age = 24;
if (name === "Bob" && age == 24) {
  console.log("Hello there user.");
} else {
  console.log("Wrong values");
}
